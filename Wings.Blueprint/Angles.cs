using System;

namespace Wings.Blueprint
{
  public static class Angles
  {
    public const float QuarterCircle = (float)Math.PI / 2f;
    public const float HalfCircle = (float)Math.PI;
    public const float FullCircle = (float)Math.PI * 2.0f;
  }
}
